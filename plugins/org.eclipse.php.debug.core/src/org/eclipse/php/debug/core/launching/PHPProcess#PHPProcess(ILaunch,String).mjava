    public PHPProcess(ILaunch launch, String name) {
        fLaunch = launch;
        fName = name;
        fTerminated = false;
        launch.addProcess(this);
        fProxy = new PHPStreamsProxy();
    }

