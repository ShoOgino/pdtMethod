        protected IStatus run(IProgressMonitor monitor) {
            runPHPWebServer(fURL, fRequestPort, fIsStopAtFirstLine);
            Logger.debugMSG("PHPDebugTarget: Calling Terminated()");
            terminated();
            return Status.OK_STATUS;
        }

