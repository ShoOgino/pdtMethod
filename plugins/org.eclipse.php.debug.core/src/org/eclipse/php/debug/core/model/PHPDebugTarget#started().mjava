	/**
	 * Notification we have connected to the debugger and it has started. .
	 */
	public void started() {
		fSuspended = false;
		if (!fIsRunAsDebug)
			installDeferredBreakpoints();
		fireCreationEvent();
	}

