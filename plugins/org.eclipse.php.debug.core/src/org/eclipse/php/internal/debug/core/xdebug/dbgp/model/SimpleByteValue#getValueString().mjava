	public String getValueString() throws DebugException {
		//TODO: Cache ?
		String valStr = Integer.toHexString(value & 0xFF);
		if (valStr.length() == 1) {
			valStr = "0" + valStr;
		}
		return valStr;
	}

