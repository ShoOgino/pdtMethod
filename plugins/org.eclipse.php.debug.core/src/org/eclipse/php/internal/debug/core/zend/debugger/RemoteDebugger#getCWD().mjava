	public String getCWD() {
		if (cachedCWD == null || !canDo(START_PROCESS_FILE_NOTIFICATION)) {
			if (canDo(GET_CWD)) {
				cachedCWD = getCWDNew();
			} else {
				cachedCWD = getCWDOld();
			}
		}
		return cachedCWD;
	}

