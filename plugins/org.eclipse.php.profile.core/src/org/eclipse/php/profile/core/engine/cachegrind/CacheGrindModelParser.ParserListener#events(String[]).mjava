		@Override
		public void events(String[] events) {
			int pos = 0;
			for (String item : events) {
				if (item.equalsIgnoreCase("Time")) { //$NON-NLS-1$
					timeIndex = pos;
					break;
				}
				pos++;
			}
		}

