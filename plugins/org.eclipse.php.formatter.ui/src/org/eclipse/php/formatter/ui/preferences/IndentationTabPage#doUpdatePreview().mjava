	/*
	 * (non-Javadoc)
	 * 
	 * @see
	 * org.eclipse.jdt.internal.ui.preferences.formatter.ModifyDialogTabPage
	 * #doUpdatePreview()
	 */
	protected void doUpdatePreview() {
		if (fPreview != null) {
			fPreview.update();
		}
	}

