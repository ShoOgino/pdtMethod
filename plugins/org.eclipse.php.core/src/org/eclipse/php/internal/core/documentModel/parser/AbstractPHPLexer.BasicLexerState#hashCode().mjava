		@Override
		public int hashCode() {
			final int prime = 31;
			int result = 1;
			result = prime * result + lexicalState;
			result = prime * result + ((phpStack == null) ? 0 : phpStack.hashCode());
			return result;
		}

