		public String[] getReturnTypes() {
			Map<String, String> info = decodeDocInfo(doc);
			if (info != null) {
				String types = info.get("r");
				if (types != null) {
					return types.split(",");
				}
			}
			return null;
		}

