		public int hashCode() {
			int h = hash;
			if (h == 0) {
				h = functionName.hashCode();
				h = 31 * h + className.hashCode();
				h = 31 * h + functionName.hashCode();
				hash = h;
			}
			return h;
		}

