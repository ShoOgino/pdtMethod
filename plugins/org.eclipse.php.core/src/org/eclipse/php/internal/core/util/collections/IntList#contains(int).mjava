	/**
	 * return true if the list contaons the 'val'.
	 */
	public boolean contains(int val) {
		for (int i = 0; i <= size; i++) {
			if (data[i] == val) {
				return true;
			}
		}
		return false;
	}

