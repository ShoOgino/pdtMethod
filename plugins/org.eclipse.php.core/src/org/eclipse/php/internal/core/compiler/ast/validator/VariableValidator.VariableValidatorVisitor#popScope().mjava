		private Scope popScope() {
			Scope tmp = scopes.pop();
			endScope(tmp);
			if (!scopes.isEmpty()) {
				current = scopes.peekLast();
			} else {
				current = null;
			}
			depth--;
			return tmp;
		}

