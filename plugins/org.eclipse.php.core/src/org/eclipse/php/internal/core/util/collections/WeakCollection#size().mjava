    /**
     * Returns the number of referenced elements in this collection.
     * If this collection contains more than <tt>Integer.MAX_VALUE</tt>
     * elements, returns <tt>Integer.MAX_VALUE</tt>.
     *
     * @return the number of referenced elements in this collection
     */
    public int size() {
        removeUnReferencedObjects();
        return refrencedCollection.size();
    }

