	public static String getType(int type) {
		switch (type) {
			case QT_QUOTE:
				return "quote";
			case QT_SINGLE:
				return "single";
			case QT_HEREDOC:
				return "heredoc";
			default:
				throw new IllegalArgumentException();
		}
	}

