	private String getConstantValue(int tag) {
		String rv = getError(tag);
		if (rv != null) {
			return '\'' + rv + '\'';
		}
		if (tag == getStringTag()) {
			return "Identifier";
		}
		if (tag == getVariableTag()) {
			return "Variable";
		}

		return null;
	}

