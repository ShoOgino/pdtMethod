	public CompletionScope(Type type, int offset, int length, ICompletionScope parent) {
		this.type = type;
		this.parent = parent;
		this.offset = offset;
		this.length = length;
		this.name = null;
	}

