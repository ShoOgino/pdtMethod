	public static String getOperator(int operator) {
		switch (operator) {
			case OP_DEC:
				return "--";
			case OP_INC:
				return "++";
			default:
				throw new IllegalArgumentException();
		}
	}

