        /**
         * We can't delegate to the hasNext() method of referencedIterator because there
         * is a possibility that all of the remaining objects in it are garbage collected.
         * Instead we're checking the next returnable object that will be returned in
         * the next next() method.
         */
        public boolean hasNext() {
            nextObject = nextImpl();
            return nextObject != null;
        }

