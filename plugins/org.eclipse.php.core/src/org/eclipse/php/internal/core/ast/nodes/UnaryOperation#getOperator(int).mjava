	public static String getOperator(int operator) {
		switch (operator) {
			case OP_PLUS:
				return "+";
			case OP_MINUS:
				return "-";
			case OP_NOT:
				return "!";
			case OP_TILDA:
				return "~";
			default:
				throw new IllegalArgumentException();
		}
	}

