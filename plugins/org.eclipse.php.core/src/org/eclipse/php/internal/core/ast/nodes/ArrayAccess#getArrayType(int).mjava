	public static String getArrayType(int type) {
		switch (type) {
			case VARIABLE_ARRAY:
				return "array";
			case VARIABLE_HASHTABLE:
				return "hashtable";
			default:
				throw new IllegalArgumentException();
		}
	}

