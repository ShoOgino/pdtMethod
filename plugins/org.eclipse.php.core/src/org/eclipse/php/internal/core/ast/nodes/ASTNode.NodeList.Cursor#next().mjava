			/* (non-Javadoc)
			 * Method declared on <code>Iterator</code>.
			 */
			public T next() {
				T result = NodeList.this.store.get(this.position);
				this.position++;
				return result;
		    }

