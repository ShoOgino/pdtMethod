	// lex to the end of the stream.
	public String lexToEnd() throws IOException {
		String curr = this.yylex();
		String last = curr;
		while (curr != null) {
			last = curr;
			curr = yylex();
		}
		return last;
	}

