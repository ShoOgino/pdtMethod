	private static int getEndingWhiteSpaces(String text) {
		int index = text.length() - 1;
		for (; index >= 0; index--) {
			char c = text.charAt(index);
			if (c != ' ' && c != '\t') {
				break;
			}
		}
		return index + 1;
	}

