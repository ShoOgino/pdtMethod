        /**
         * We check that nextObject is non null in case there is a next() call after
         * a next() call without hasNext() in between.
         */
        public Object next() {
            if (nextObject == null) {
                nextObject = nextImpl();
            }
            Object rv = nextObject;
            nextObject = null;
            return rv;
        }

