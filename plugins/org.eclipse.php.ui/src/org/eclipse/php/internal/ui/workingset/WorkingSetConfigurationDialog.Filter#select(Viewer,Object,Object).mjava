		public boolean select(Viewer viewer, Object parentElement, Object element) {
			IWorkingSet ws = (IWorkingSet) element;
			String id = ws.getId();
			return HistoryWorkingSetUpdater.ID.equals(id) || OthersWorkingSetUpdater.ID.equals(id) || PHPWorkingSetUpdater.ID.equals(id);
		}

