	private void unpackIsSupportingASPTags() {
		String value = getValue(PREF_ASP_TAGS);
		isSupportingASPTagsButton.setSelection(Boolean.valueOf(value).booleanValue());
		versionComboListener.widgetSelected(null);
	}

