	/* (non-Javadoc)
	 * @see org.eclipse.php.internal.ui.search.PHPSearchScope#add(org.eclipse.core.resources.IResource)
	 */
	public void add(IResource resource) {
		IProject project = resource.getProject();
		add(project);
	}

