	public void uninstall() {
		if (timer != null) {
			timer.stop();
			timer = null;
		}
		if (isInstalled()) {
			projectionDisabled();
			viewer.removeProjectionListener(this);
			viewer = null;
		}
	}

