	private int handleValidateMove(Object target, DropTargetEvent event) {
		if (target == null)
			return DND.DROP_NONE;

		if (!canMoveElements())
			return DND.DROP_NONE;

		return DND.DROP_NONE;
	}

