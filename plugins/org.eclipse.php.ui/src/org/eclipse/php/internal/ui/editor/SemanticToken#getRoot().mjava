	/**
	 * @return the AST root
	 */
	public Program getRoot() {
		if (!fIsRootResolved) {
			fIsRootResolved= true;
			fRoot= (Program) (fNode != null ? fNode : fLiteral).getRoot();
		}

		return fRoot;
	}

