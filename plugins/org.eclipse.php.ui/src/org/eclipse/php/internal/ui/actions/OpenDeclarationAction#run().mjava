	/* (non-Javadoc)
	 * @see org.eclipse.jface.action.IAction#run()
	 */
	public void run() {
		if (!isEnabled()) {
			return;
		}
		if (validAction()) {
			PHPCodeHyperLink link = new PHPCodeHyperLink(null, fCodeDatas);
			link.open();
			return;
		}
	}

