	public void reset() {
		// do not reset when an AST is under construction
		if (fWaitLock.tryLock()) {
			synchronized (this) {
				fAST = null;
				fState = STATES.NONE;
			}
			fWaitLock.unlock();
		}
	}

