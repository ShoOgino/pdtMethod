	/**
	 * @param var
	 *            Variable to get
	 * @return Variable's value
	 */
	public String get(final String var) {
		if (vars == null) {
			return ""; //$NON-NLS-1$
		}
		final Object val = vars.get(var);
		if (val == null) {
			return ""; //$NON-NLS-1$
		}
		return val.toString();
	}

