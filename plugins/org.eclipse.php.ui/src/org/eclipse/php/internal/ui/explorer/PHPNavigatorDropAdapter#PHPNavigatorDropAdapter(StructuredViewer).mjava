    /**
     * Constructs a new drop adapter.
     * 
     * @param viewer the navigator's viewer
     */
    public PHPNavigatorDropAdapter(StructuredViewer viewer) {
        super(viewer);
        fReorgMoveAction =(IPHPMoveActionDelegator)PHPActionDelegatorRegistry.getActionDelegator(MOVE_ACTION_ID);
    }

