	/**
	 * @param existingPHPFile - The existing php file object in which we want to inject the new element
	 * @param offset - The StructuredDocument offset from which we inject the code
	 */
	public CodeInjector(PHPFileData existingPHPFile, int offset) {
		this.existingPHPFile = existingPHPFile;
		this.offset = offset;
	}

