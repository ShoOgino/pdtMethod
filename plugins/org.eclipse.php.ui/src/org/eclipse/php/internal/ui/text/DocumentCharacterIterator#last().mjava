	/*
	 * @see java.text.CharacterIterator#last()
	 */
	@Override
	public char last() {
		if (fFirst == fLast) {
			return setIndex(getEndIndex());
		} else {
			return setIndex(getEndIndex() - 1);
		}
	}

