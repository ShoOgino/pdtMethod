	/*
	 * @see IColorManager#getColor(String)
	 */
	@Override
	public Color getColor(String key) {

		if (key == null) {
			return null;
		}

		RGB rgb = fKeyTable.get(key);
		return getColor(rgb);
	}

