	private boolean inputDeleted() {
		if (fInput == null)
			return false;
		if ((fInput instanceof IResource) && ((IResource) fInput).exists())
			return false;
		postRefresh(fInput);
		return true;
	}

