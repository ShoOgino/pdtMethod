	public void setPort(String port) {
		try {
			if (port.equals("")) {
				setAttribute(Server.PORT, "80");
			} else {
				setAttribute(Server.PORT, port);
			}
		} catch (Throwable e) {
			;
		}
	}

