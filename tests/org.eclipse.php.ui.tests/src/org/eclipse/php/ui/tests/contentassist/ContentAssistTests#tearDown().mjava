	@After
	public void tearDown() throws Exception {
		if (testFile != null) {
			testFile.delete(true, null);
			testFile = null;
		}
		if (otherFiles != null) {
			for (IFile f : otherFiles) {
				f.delete(true, null);
			}
			otherFiles = null;
		}
	}

