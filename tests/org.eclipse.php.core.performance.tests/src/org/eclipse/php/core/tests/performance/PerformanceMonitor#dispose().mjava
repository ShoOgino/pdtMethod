	/**
	 * Global destruction method, which should be called when testing plug-in
	 * stops.
	 */
	public void dispose() {
		try {
			pool.dispose();
		} catch (Exception e) {
			e.printStackTrace();
		}
	}

