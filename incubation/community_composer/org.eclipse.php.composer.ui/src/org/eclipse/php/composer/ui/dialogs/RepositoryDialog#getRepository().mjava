	public Repository getRepository() {
		if (repository != null) {
			return repository;
		}

		Repository repo = RepositoryFactory.create(type);
		repo.setUrl(url);
		repo.setName(name);

		return repo;
	}

