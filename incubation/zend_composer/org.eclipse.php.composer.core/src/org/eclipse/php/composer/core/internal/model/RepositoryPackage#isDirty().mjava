	@Override
	public boolean isDirty() {
		boolean result = isDirty;
		if (!result) {
			result = getDist().isDirty();
			if (!result) {
				result = getSource().isDirty();
			}
		}
		return result;
	}

